name: Keep Supabase Alive

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase project
        run: |
          curl -s -o /dev/null -w "%{http_code}" \
            "https://esqfwlzjquynsjsdsuuj.supabase.co/rest/v1/projects?select=count" \
            -H "apikey: sb_publishable_608ZEifNav8TYl-wHKAZuw_Q7T35MWg"
